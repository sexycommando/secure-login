Spring MVC secure login sample
===============

Web Multi Project (MyBatis3) using TERASOLUNA Server Framework for Java (5.3)

## Usage
- how to build  
    mvn clean package -P test-server  
    mvn clean package -P production-server  
